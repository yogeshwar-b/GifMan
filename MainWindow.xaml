<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="GifMan.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GifMan"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="GifMan">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid ColumnDefinitions="2*,Auto" ColumnSpacing="8" Margin="10,20,10,0">
        <TextBox x:Name="FilePathTextBox"
                     PlaceholderText="Select File.."
                     IsReadOnly="True"
                 IsHitTestVisible="False"
                 SelectionHighlightColor="Transparent"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Top"  
                 Grid.Column="0"

                 />
        <Button Name="FilePathSelectButton"
                Content="Choose File" Click="FilePathSelectButton_Click"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"  
                Grid.Column="1"/>

    </Grid>
</Window>
